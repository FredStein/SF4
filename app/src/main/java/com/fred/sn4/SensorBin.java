package com.fred.sn4;

/**
 * Created by Fred Stein on 14/04/2017.
 */

class SensorBin implements Runnable{
        long binSize;
        SensorBin(long binSize) {
            this.binSize = binSize;
        }
        public void run() {
            // compute primes larger than minPrime
        }
}
